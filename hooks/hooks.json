{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | { read -r f; [ -n \"$f\" ] && bbu coupling-guard \"${f#$PWD/}\" 2>/dev/null; } || true",
            "timeout": 15
          }
        ]
      }
    ]
  }
}
